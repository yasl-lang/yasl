language: c

compiler:
  - clang
  - gcc

before_script:
  - cmake .
  - make YASL
  - make YASLTEST

script: 
  - ./YASLTEST                    # run c tests
  - ( cd test ; perl vmtest.pl )  # run perl tests

notifications: 
  email: false 
